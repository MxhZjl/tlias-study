1.sql语言分为DDL，DML，DQL，DCL。

2.DDL语言是用来定义的

3.数据库当中，字段指的是列，数据是行

4.数值类型的选取规则：在满足业务需求的前提下，尽可能选择占用磁盘空间小的数据类型

5.数据库当中varchar和text的区别

选型建议（新手直接套用）

优先用 VARCHAR：如果文本长度≤5000 字符（日常大部分场景），选 VARCHAR，性能更好、更灵活；

&#x20;选 TEXT 的情况：文本长度 > 5000 字符，或不确定长度但大概率很长（如文章、详情）；

&#x20;特殊场景：如果文本超 65535 字符（比如几万字的文档），直接用 MEDIUMTEXT，不用纠结 VARCHAR。

&#x20;VARCHAR 和 TEXT 都支持任意字符（中文 / 英文），核心区别是存储上限、存储位置、默认值 / 索引规则；

&#x20;短 / 中等长度文本（<5000 字符）用 VARCHAR（性能优、支持默认值），长文本用 TEXT（容量大、不占行长度）；

&#x20;TEXT 建索引必须指定长度，且 MySQL 中 TEXT 不支持默认值，这是新手最容易踩的坑。

6.日期数据类型建议

&#x20;  date：只要年月日

&#x20;  datetime:除了年月日还需要时分秒

&#x20;  time：时分秒

7.对于像图片，音频，视频这样的资源，是不会直接存储到数据库当中的，数据库当中存储的是这些资源所在的路径，这样就可以通过路径从而找到这些资源

8\.像性别 职位这些有选项可以给人们选的，数据库存储的一般是1,2,3,4这样的，规定什么数字代表什么，然后具体最终页面显示的是男士还是男生，就不是数据库该管的了，而是我暂时觉得是前端工程师该管的、

9\.在创建表结构的时候，除了要有必须得业务字段，还要考虑基础字段，比如ID主键，创建时间，最后更新时间





